# 公告管理模組 — 規劃與實作現況

> 狀態：業務邏輯**已與客戶確認（2026-07-31）**，定案流程已併入 `business-logic.md`「公告管理」節。
> 後台第一版可操作畫面已做；**確認後的模型調整（審查鏈、文號改案件層級、解耦、已審結）
> 尚待實作**（見文末「待建置」）。前台公告專區顯示頁維持舊版，最後做。
>
> 建立日期：2026-07-23｜最後更新：2026-07-31

---

## 一、問題定位

系統內三條主線的最後一關都回到醫事司，準備公告前置作業：

| 來源模組 | 路由 | 進入待公告的條件 | 案件粒度 | 量級 |
|---------|------|---------------|---------|------|
| 填報文件審查 | `/review/submissions` | 案件推進至 `pending-announcement` | 文件類型 × 醫學會 | 6 × 25 ＝ 最多 150 |
| 醫院容額分配審查（即容額填報的審查面） | `/review/hospital-quota` | 案件推進至 `upload-pending` | 醫學會（內含各醫院容額表） | 25 |
| 外加容額管理 | `/filing/additional-quota` | 登錄審查結果後轉「待公告」 | 申請項目 uid（院 × 分科） | 全年滾動，數十筆 |

> 容額成果報告（`/review/outcome-report`）終點是**歸檔**，不產生公告；外加容額成果報告
> （`/filing/outcome-report`）終點是**留評論歸檔**，亦不產生公告。兩者不進此模組。

現況實作把「待公告案件」放在 `/announcement-management` 頁首的鈴鐺 **Popover** 裡，
硬編 5 筆假資料，且每筆案件對應一顆「建立公告」按鈕。

**兩個結構性問題：**

1. **量級不符**：一次審查會議後可能同時湧入上百筆待公告案件，Popover 放不下、無法篩選、
   無法批次處理，數量為 0 時整個入口消失（沒有空狀態）。
2. **對應關係錯誤（更關鍵）**：醫事司不會發 150 份公告。實務是**一份公告彙整多筆案件**
   —— 例如「115 年度專科醫師訓練計畫認定基準修訂公告」一次涵蓋 25 個醫學會的認定基準，
   名單與內容放附件。`business-logic.md` 的外加容額節也已寫明「公告為跨案件的滾動彙整，
   **非單案欄位**」。現況的 1 案 1 公告是錯的模型。

---

## 二、建議：案件 → 公告是「多對一」，中間要有「組稿」這一關

```
審查完成 → 案件進「待公告」池
      ↓（醫事司在待公告工作台勾選 N 筆，可跨醫學會、同一文件類型）
建立公告草稿：帶入來源案件、自動生成名單表、帶入案件附件
      ↓
編輯內容 → 預覽 → 填入公文文號與發文日期
      ↓
發布 → 公告上架前台；同時回寫所有來源案件為「已公告」＋公告文號／日期／公告連結
```

三個名詞先定義清楚，後面才不會混淆：

- **案件（Case）**：來源模組的一筆審查案，有自己的階段。
- **公告（Announcement）**：一份對外文件，涵蓋 0～N 筆案件。
- **組稿（Compose）**：從待公告池挑案件 → 生出一份公告草稿的動作。

### 案件端需要新增的中間狀態

`待公告 → 已納入公告草稿 → 已公告`

沒有中間狀態的話，同一批案件會被兩份草稿重複收錄。建議草稿收錄即鎖定，草稿刪除則釋回。

### 也要保留「不從案件來」的公告

作業時程公告、說明會通知、申請開放公告等屬醫事司自建，沒有來源案件。
所以「新增公告」按鈕保留，公告有 `來源：案件彙整 / 自建` 兩型。

---

## 三、頁面結構建議

沿用 `/filing/additional-quota` 的頁內 tab 版型（不新增第二層 nav，符合設計規範），
公告管理拆成兩個平行路由、共用頁首 tab 列：

| 路由 | 內容 |
|------|------|
| `/announcement-management/pending`（**預設落地頁**） | 待公告案件工作台 |
| `/announcement-management` | 公告清單（草稿／已排程／已發布／已下架） |
| `/announcement-management/compose` | 公告編製（組稿）：來源案件 → 內容 → 附件 → 預覽 → 發布 |
| `/announcement-management/[id]` | 管理端公告檢視：內容 ＋ 涵蓋案件清單 ＋ 回寫狀況 |

理由：待公告工作台是醫事司的日常入口，量大且需篩選與批次操作，值得獨立路由以便從
審查頁深連結（「本案已進待公告 → 前往辦理公告」）。

### 3-1 待公告案件工作台

- **三個來源 tab**（填報文件審查｜醫院容額分配｜外加容額），各帶未處理筆數。
  跨來源不併批 —— 三者公告文書格式不同，同一份公告不會混。
- 篩選：年度、文件類型、醫學會／醫院、通過日期區間、關鍵字。
- 表格欄位：勾選框、案件名稱、來源、文件類型、醫學會／醫院、審查通過日、目前狀態、查看審查。
- 批次動作列（選取後浮現）：**彙整為公告**（→ 組稿頁）、**延後至下批**、**查看審查**。
- 現況那顆 `X`「忽略」要拿掉，換成有狀態、可還原的「延後至下批」，並記錄原因。
  否則案件會被靜默丟棄，永遠卡在待公告池外。
- 空狀態要有：「目前沒有待辦公告案件」。

### 3-2 公告編製（組稿）

四段式，單頁分區（不用 wizard，醫事司需要來回改）：

1. **來源案件**：帶入勾選的案件，可再增刪；顯示合計筆數與涵蓋醫學會。
2. **公告內容**：標題、年度、類別、公文文號、發文日期、生效／施行日期、正文。
   正文下方提供**自動生成的名單表**（醫院／分科／核定容額，取自案件資料）可插入或匯出。
3. **附件**：從來源案件已上傳檔案勾選帶入（會議紀錄、核定名單、基準對照表），
   ＋自行上傳。帶入採**快照**而非連結：公告發布後，案件端檔案再異動不應改到已發布公告。
4. **預覽**：以前台公告樣式呈現，確認後發布。

### 3-3 公告清單

保留現有表格，但欄位改為：置頂、標題、類別、年度、狀態、發文日期、涵蓋案件數、附件數、操作。
移除「瀏覽數」（mock 指標，醫事司不看，佔欄寬）。

---

## 四、已定案的決策

### 4-1 初版就定案（2026-07-23）

| # | 決策 | 實作方式 |
|---|------|---------|
| 1 | **公文文號人工輸入**，系統不做文號管理，就是一串字串；**沒有文號不得發布** | 組稿頁必填欄位；`canPublish()` 檢核 |
| 2 | **更正公告**採版本鏈：原公告不動，另建更正稿，更正稿發布後才標記取代 | `createCorrection()`；公告詳情頁顯示「更正自／已由 X 取代」 |
| 3 | **排程先做簡約版**：填未來上架日期＝「已排程」，**不做自動下架** | 狀態 `草稿／已排程／已發布／已下架`，下架一律手動 |
| 4 | **前後台類別體系統一** | 收斂為 `training-document／hospital-quota／additional-quota／general`，前台 `/announcements` 同步改用 |

### 4-2 客戶討論後定案（2026-07-31）

| # | 決策 | 對現況的影響 |
|---|------|-------------|
| 5 | 審查鏈**新增「醫事司審查」階段**（RRC 後、審查通過前），可退件回醫學會 | 改三線 stage 模型 |
| 6 | 審查終點＝醫事司點「**審查通過**」＝交棒公告管理；**審查專區移除「待公告／已公告」階段** | 「待公告」全系統只留在公告管理一處 |
| 7 | 待公告後兩條**獨立**下一步：**系統內公告**（最簡、一篇可涵蓋多案）／**官網公告文件**（每醫學會一份） | 以 signifier 呈現每案「還缺哪步」，不綁死順序 |
| 8 | **文號／修正歷程改掛在案件（醫學會）層級**，非公告批次；官網文件以醫學會為單位產出（容額、文件審查皆是） | 推翻初版「一公告一文號」；資料模型要改 |
| 9 | 審查專區內新增**「已審結」唯讀歸檔**（案件層級：審查歷程＋文件＋公告文號歷程） | 新頁面 |
| 10 | **移除 `/document-archive`** | ✅ 本輪已做 |
| 11 | 產出／下載**一律純按鈕、不做真下載／真產檔**（後端負責；避免跨 session 讀碼誤會） | 需拆掉現有 `.txt/.csv` 真下載 |

### 4-3 仍待與客戶確認（優先度低）

1. **發文日期 / 上架日期 / 生效日期**三者是否都要？目前三個都做了，可刪。
2. **一份公告可否跨文件類型**？目前**不擋、只警示**（組稿頁偵測到多種項目時跳提醒）。
3. **公告後是否要通知**相關醫學會／醫院（站內通知或 email）？目前未做。
4. **受文者／正副本**要不要在系統內管理？（可能屬公文系統職責）目前未做。
5. **稽核歷程**是否需要核稿／會簽這一關？目前只記「誰、何時、做了什麼」。
6. **延後的案件**是否需要期限或提醒？目前只記原因、可隨時還原。
7. **公告下架後**前台是否完全不可見，還是保留為歷史查詢？目前是完全不可見。

## 五、施作進度

### Phase B — 審查側改造（✅ 已完成 2026-07-31，三線皆已對齊）

- [x] **審查 stage 模型（三線）**：新增「醫事司審查」、移除「待公告／已公告」，終點改「審查通過」。
      - 容額填報審查：`quota-filing-stage` / `review-hospital-quota` 改為 醫策會初審→分組會議→RRC大會→醫事司審查→審查通過。
      - 文件填報審查：各文件類型鏈終點改 `passed`；有分組／RRC 者於 RRC 後加 `ministry-review`。
      - 外加容額（全線一致）：階段收斂為 待審查／審查通過；「已公告」改由 `announcementDate` 判斷，
        並解除外加容額成果報告模組對 `stage==="已公告"` 的依賴。
- [x] **待公告池來源**：`announcement-cases.ts` 三來源皆改抓「審查通過」案件。
- [x] **已審結唯讀歸檔**：容額填報審查新增「已審結」分頁（顯示公告端狀態＋回查審查歷程）；
      文件填報審查以「審查通過」終點篩選呈現。

### Phase A — 公告側改造（✅ 已完成 2026-07-31）

採客戶確認的「製作→發布」模型（見 `business-logic.md`）：審查通過 → 待製作公告檔案 →
製作（官網公告文件，文號手動輸入）→ 已製作 → 發系統內公告引用檔案 → 已公告。

- [x] **公告檔案資料模型**（`announcement-cases.ts`）：`OfficialDoc`（文號堆疊掛案件層級）＋
      `publishedPostId`；狀態衍生 待製作／已製作／已公告／已延後；`isCaseAnnounced` 供反查。
- [x] **待公告工作台**：兩個 signifier 欄（官網公告文件／系統內公告）＋批次動作
      「製作公告檔案」（對話框：發文日期＋手動文號＋比照 ref 的套版預覽）、「發系統內公告」、延後／還原。
- [x] **官網公告文件產出**：對話框套版預覽，**純按鈕、不做真產檔**。
- [x] **系統內公告發布**：組稿頁發布即 `publishReferencedCases`（引用已製作檔案 → 案件已公告）；
      系統內公告本身不需文號（文號在案件的公告檔案上）。
- [x] **拆掉真下載**：移除 `lib/announcement-export.ts`，公告文稿／名單下載改 toast 示意。
- [x] **外加容額 re-plumb**：移除案件的 `announcementDate/Number` 欄位與 UI；外加容額成果報告
      改**反查公告管理**（`isCaseAnnounced`，文號／日期取自 `officialDoc`）；審查通過頁改中性指路。

**驗證（瀏覽器）**：製作 2 份 → 待製作↓／已製作↑、新文號入表；發系統內公告 → 案件轉已公告、
系統內公告 signifier 轉已發布；外加容額成果報告 4 筆（文號取自 officialDoc）；外加容額列表無公告狀態。

**已知 mock 限制**：資料為 module-level singleton，**整頁重新載入會重置**（SPA 內導航則保留）。

### 尚可再收斂（非阻斷，未做）

- [ ] 組稿頁仍保有部分「彙整為公告」時期的框架（自身的文號欄位、涵蓋案件語彙）；可再貼近
      「系統內公告引用已製作檔案」的敘事。
- [ ] 官網公告文件的「更正」（`addOfficialCorrection` 已備好）尚未接 UI 入口。
- [ ] 已製作檔案的套版預覽目前為表頭示意；醫院容量表未由審查結果實際帶入。

> 註：`app/statistics` 儀表板仍有寫死的「待公告／已公告」統計標籤（非由階段模型驅動的假數字），
> 屬 mock 展示數據，待 Phase A 或統計改版時一併對齊。

---

## 六、待公告工作台調整（2026-07-31 會議）

> 討論後統一實作。**簡單項先列於此**；複雜項（IA、分類、檢視審查目標頁）討論定案後補入，
> 並回頭確認與簡單項的耦合，最後一起改。

### 簡單項（✅ 已完成 2026-07-31，與複雜項一起改）

- [x] **主體欄「醫學會」改為「科別」**＋科別篩選（PendingCase 新增 `specialty`，醫學會來源由
      `societies.specialty` 帶出，外加容額用分科）。
- [x] **延後功能整個移除**（deferCases/restoreCases、deferReason、已延後狀態全拿掉；未處理案件留在列表）。
- [x] **移除統計數字卡片區塊**。
- [x] **列表直接顯示公告文號**（已製作/已公告階段各一欄）。
- [x] **顯示系統內公告日期**（已公告階段一欄，`publishedDate`）。
- [x] **已製作/已公告案件加「下載」按鈕**（純按鈕、toast 示意、不做真下載）。

> 耦合提醒：文號顯示(#5)、系統內公告日期(#6)、下載按鈕(#11)、已製作/未製作區分(#4)的**擺放位置**
> 取決於下方 IA 決策（是否分成第二層 tab）；故簡單項的「做不做」已定，「放哪」等 IA 定案。

### 複雜項（2026-07-31 討論定案並實作）

> 再議後結論：**保留系統內公告狀態、已公告案件不移出**（使用者製作後仍需下載去官網、且需知道
> 是否已完成系統內發布）。改以**階段分頁**引導：來源 tab × 階段分頁（待製作／已製作·待發布／已公告）。
> 「是否已發布」由所在階段直接表達，不再是混淆的欄位。**已實作並驗證。**

- [x] **IA 重整（#9）→ 依「使用者任務作業階段」以兩層結構引導**。兩個任務 story 分開：
      - **公告文件製作**（現「待公告案件」tab）：**只管製作**。來源 tab（文件/容額/外加）＋
        每案「待製作／已製作」以**欄位/signifier**呈現（不加狀態 tab，避免層級過多）。
        **移除**系統內公告（發布）欄、統計卡片、延後。**已公告案件移出本工作台**（已交給公告清單）。
      - **公告清單**（現「公告清單」tab）：操作起點只有 (1) 新增公告 (2) 點一個公告檢視/編輯。
      - **銜接**：於製作工作台**選取多筆「已製作」的公告文件 → 帶入「新增公告」頁**（沿用現有
        compose，seeded）。發布屬公告清單側；**「已發布/未發布」不再出現在製作工作台**（解決 signifier 混雜）。
      - #4「已製作/未製作區分」由此結構性解決。
- [x] **分類（#2）**：指來源 tab。**外加容額與（未來的）容額微調同屬一個文件分類**。容額微調尚未實作，
      故現在**不動結構**，僅保留：日後容額微調的待公告案件併入「外加容額」這一 tab（或該分類）。
- [x] **檢視審查目標頁（#3＋#10）→ 選 (A)**：修既有詳情頁，審查通過時呈現**真唯讀**。
      **真唯讀＝把變更狀態的控制 UI 從畫面移除（不是 disable）**。
      - 容額填報審查詳情（`/review/hospital-quota/[id]`）：已達真唯讀（審查操作 Card 未 render）✅
      - **文件填報審查詳情（`/review/[id]`）：待修** —— 審查通過仍 render 審查結果 select／評語／
        儲存／上傳，且右上「審查結果」顯示過時「待審查」。改為審查通過時**不 render** 這些控制。
      - 外加容額詳情：表單唯讀＋指路，OK。

### 簡單項因 IA 定案而調整的耦合

- **#6 系統內公告日期**：製作工作台不再顯示發布資訊，故此項**移到公告清單側**呈現（每則公告的發布日）。
- **#5 文號、#11 下載按鈕、#4 已製作/未製作**：都落在製作工作台的「已製作」列上。
- **#7 延後**：整個功能移除（deferCases/restoreCases、deferReason、已延後狀態、延後對話框全拿掉）；
  未處理的案件就一直留在工作台。
- **#8 統計卡片**：移除。
- **#1 科別**：主體欄改「科別」。文件/容額來源由醫學會的 `specialty` 帶出；外加容額本以分科為主體。

---

## 六、本次已實作

### 路由

| 路由 | 內容 |
|------|------|
| `/announcement-management/pending` | 待公告案件工作台：三來源切換＋統計＋篩選＋多選批次 |
| `/announcement-management` | 公告清單：篩選（類別／狀態／年度／關鍵字）＋檢視／編輯／更正／下架／刪除草稿／匯出 |
| `/announcement-management/compose` | 公告編製：涵蓋案件 → 內容 → 公文資訊 → 附件 → 預覽 |
| `/announcement-management/[id]` | 管理端公告檢視：公文資訊、內容、附件、涵蓋案件、版本鏈、作業歷程 |

`create/` 與 `[id]/edit/` 已移除（功能併入 `compose`，避免三處各自維護一份表單）。

### 資料層

- `lib/mock/announcement-cases.ts`：待公告案件池，**由三個來源模組實際衍生**（不是另外編一份假資料），
  案件狀態 `待公告 → 公告編製中 → 已公告`，另有 `已延後`。
- `lib/mock/announcements.ts`：公告模型、狀態機、更正版本鏈，並保留前台相容層。
- `lib/announcement-export.ts`：公告文稿與涵蓋案件名單的實際檔案下載。

### 版型／實作缺陷修正（前一個 agent 留下的）

- [x] 篩選列三欄參差。根因：本專案 shadcn 版本的 `SelectTrigger` 預設帶 `w-fit`
      （`components/ui/select.tsx:40`），舊版把它放進 `grid md:grid-cols-3` 卻沒給寬度。
      已改為 `/filing/additional-quota` 那種 inline 工具列並明確指定寬度。
- [x] 篩選區、表格不再包 `<Card>`，統一為 `rounded-lg border bg-white` ＋ `overflow-x-auto`。
- [x] 操作欄改為有文字的「檢視」按鈕＋具名下拉選單；刪除草稿有二次確認並釋回案件。
- [x] 主要內容改用 `text-base`，表格列高 `h-14`；`text-sm` 只留在時間戳與輔助說明。
- [x] 頁首改用共用的 `PageHeader`，標題／副標／按鈕基線一致；移除鈴鐺紅點與 Popover。
- [x] 移除「瀏覽數」欄，改為「涵蓋案件」與「公文文號」。
- [x] 後台不再內嵌 local mock，前後台共用 `lib/mock/announcements.ts` 與同一組類別 key。
- [x] 補上 `components/layout/page-container.tsx`（`PageContainer` / `PageHeader`），
      讓 `DESIGN_GUIDELINES.md` 指定的元件真的存在；新頁一律使用，舊頁逐步遷移。

### 已知限制（mock 原型）

- 資料存在 module-level singleton，**整頁重新載入會重置**（與專案既有 mock 一致）。
- 案件在「儲存草稿／發布」時才鎖定為公告編製中，不是一進組稿頁就鎖。
- 待公告案件的「審查通過日」沿用來源模組 mock 的日期，部分為 114 年度，與公告的 115 年度
  不完全對齊 —— 屬上游 mock 資料，未動。
